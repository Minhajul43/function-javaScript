function differ(num1, num2) {
  if (num1 > num2) {
    let difference = num1 - num2;
    console.log(num1, '-', num2, '=', difference);
  }
  else {
    let difference = num2 - num1;
        console.log(num2, '-', num1, '=', difference);
  }

}

differ(434, 542);

